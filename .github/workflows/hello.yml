name: Hello # ワークフロー名 # 省略が可能

on: push # イベント（プッシュ時に起動）

jobs: # ジョブの定義
  hello: # ジョブID
    runs-on: ubuntu-test #ランナー（Ubuntuで実行）
    steps: # ステップの定義
      - run: echo "Hello, world" #シェルコマンドの実行
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2 # アクションの呼び出し

